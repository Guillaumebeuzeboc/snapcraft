name: colcon-ros2-content-sharing
version: "1.0"
summary: hello world
description: |
  A ROS2 rlcpp-based workspace.

grade: stable
confinement: strict
base: core20

apps:
  colcon-ros2-content-sharing:
    command: ros2 run colcon_ros2_content_sharing colcon_ros2_content_sharing
    plugs: [network, network-bind]
    extensions: [ros2-foxy-ros-base]

parts:
  hello:
    plugin: colcon
    source: .
    build-packages: [g++, make]
